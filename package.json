{
  "name": "website",
  "private": true,
  "description": "Personal site — writings, photos, about",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-photos.js && astro build",
    "preview": "astro preview",
    "photos": "node scripts/generate-photos.js"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "astro": "^6.4.5"
  },
  "devDependencies": {
    "@types/node": "^25.9.2"
  }
}
